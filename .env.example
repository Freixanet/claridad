# Client (Expo app) — URL of the deployed Vercel proxy (no trailing slash)
EXPO_PUBLIC_API_URL=https://your-project.vercel.app

# Server (Vercel only) — Gemini API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
